<%= link_to bookmark_path(current_user.bookmarks.find_by(board_id: board.id).id), id: "unbookmark-button-for-board-#{board.id}", data: { turbo_method: :delete } do %>
  <i class="bi bi-heart-fill" style="color: red; font-size: 30px;"></i>
<% end %>
